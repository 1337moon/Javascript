// javascript code
const text = [1, "moon", "noob", "sagor", true];

function countStrings(arr) {
    let count = 0;
    for (let i = 0; i < arr.length; i++) {
        if (typeof arr[i] === "string") {
            count++;
        }
    }
    return count;
}

console.log(countStrings(text));
