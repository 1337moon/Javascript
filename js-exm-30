// javascript code
function countChar(str, char) {
    let count = 0;
    for (let ch of str) {
        if (ch === char) count++;
    }
    return count;
}
  
let text = "banana";
console.log(countChar(text, "a"));
