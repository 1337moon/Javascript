// javascript code
function arrayDouble(arr) {
    let newArr = [];
    for (let i = 0; i < arr.length; i++) {
        newArr.push(arr[i] * 2);
    }
    return newArr;
}

console.log(arrayDouble([1, 2, 3, 4, 5]));
